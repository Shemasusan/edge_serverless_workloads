apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis-server
  name: redis
  namespace: default
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis-server
  type: ClusterIP
